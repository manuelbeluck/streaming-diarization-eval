# CallHome English Dataset Configuration

dataset:
  name: callhome
  language: eng                        # Language: 'eng', 'deu', etc.
  path: data/callhome                  # Cache directory
  # recordings: [0, 1, 2, 3, 4]        # Optional: specific recording indices

systems:
  - name: diart
    chunk_size: 0.5

evaluation:
  collar: 0.25

output_dir: ./results/callhome_eng
